package com.hablapps.cirbe.test

import org.scalacheck._
import org.scalacheck.Properties
import org.scalacheck.Prop.{ forAll, BooleanOperators }

object CirbeCheck extends Properties("Cirbe") {

  // property("") = forAll { (a: Int) =>
  //   a == 0
  // }
}
